# TODO — Future Improvements
- Find and go to events
- Add new test user that logs in without onboarding (all the choices should be pre-made). When this test user creates a date request, there should be 3 more users that create a request as well. Make these 3 users similar so we can test matching. (4 accounts in total to test groupchat and features after matching)
- Date requests should be editable. Make sure that everything that was chosen can be changed later on
- Looks rating and looks based matching?