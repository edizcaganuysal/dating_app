PHASE_NAME="Authentication System"
TEST_CMD="cd backend && python -m pytest tests/ -v --tb=short 2>&1"
COMMIT_MSG="phase-02: auth system with university email verification, JWT login, protected routes"
