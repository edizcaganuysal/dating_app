services:
  postgres:
    image: postgres:16-alpine
    container_name: lovegenie-postgres
    environment:
      POSTGRES_USER: lovegenie
      POSTGRES_PASSWORD: lovegenie
      POSTGRES_DB: lovegenie_dev
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lovegenie -d lovegenie_dev"]
      interval: 2s
      timeout: 5s
      retries: 15

volumes:
  pgdata:
